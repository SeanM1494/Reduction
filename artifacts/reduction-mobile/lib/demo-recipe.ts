// One-line shim: the guacamole fixture lives once, in data/demoRecipe.ts
// (ported verbatim from the web demo). This path is kept so nothing churns.
export { DEMO_RECIPE } from '@/data/demoRecipe';
