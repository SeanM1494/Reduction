// One-line shim: the model lives once, in @workspace/recipe-model — the same
// pattern the web and api artifacts use. This was the last local copy; folded
// in as the first act of Phase 0 (see ROADMAP, mobile rebuild). No logic here.
export * from "@workspace/recipe-model/sync";
