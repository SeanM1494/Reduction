// One-line shim: the model lives once, in @workspace/recipe-model. Kept so
// existing "../shared/progress" imports did not have to churn. Do not add logic.
export * from "@workspace/recipe-model/progress";
