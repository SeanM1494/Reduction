import "./_group.css";
import { ReductionPage } from "./_shared/ReductionPage";

// Baseline: the app exactly as it looks today (Bricolage Grotesque +
// JetBrains Mono, warm cream/tan palette, terracotta + sage accents).
export default function Current() {
  return <ReductionPage />;
}
